[project]
name = "dashboards"
version = "0.1.0"
description = ""
authors = [
    {name = "locinone",email = "locinone@protonmail.com"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "streamlit (>=1.45.0,<2.0.0)",
    "streamlit-elements (>=0.1.0,<0.2.0)"
]

[tool.poetry]
packages = [{include = "dashboards", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
